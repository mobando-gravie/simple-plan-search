# Neon Postgres — the plan cache and Gravie modifier tables live here.
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Ideon (formerly Vericred) individual-market API.
IDEON_API_KEY=
IDEON_BASE_URL=https://api.ideonapi.com

# Password gate (comma-separated list of accepted passwords).
APP_PASSWORDS=change-me
# 32+ random bytes, hex. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
SESSION_SECRET=

# Optional: comma-separated shared secrets for headless agents. A caller off the IP
# allowlist sends one as `Authorization: Bearer <token>` (or `X-Api-Key`). Empty
# disables the bearer path entirely.
API_TOKENS=

# Comma-separated IPs or CIDRs that bypass the password gate (v4 + v6).
ALLOWED_IPS=127.0.0.1,::1

# Cache freshness for an Ideon plan search, in seconds.
PLAN_CACHE_TTL_SECONDS=86400

# Optional: base URL of an ichra-shopping instance, for `npm run compare --baseline-url`.
SHOPPING_BASE_URL=
