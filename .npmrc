# Public registry, pinned per-repo. The machine-level ~/.npmrc points at Gravie's
# CodeArtifact mirror, which bakes private URLs into package-lock.json and makes
# the lockfile unusable anywhere without those credentials (Vercel returns E401).
# No dependency here is private.
registry=https://registry.npmjs.org/
